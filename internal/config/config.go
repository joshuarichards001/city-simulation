package config

const (
	// Dimensions of the generated city grid.
	CitySize = 20

	// The number of citizens to generate when not loading from a file.
	CitizenCount = 10

	// Default data file locations.
	CitizensFilePath = "data/citizens.json"
	CityFilePath     = "data/city.json"
)
